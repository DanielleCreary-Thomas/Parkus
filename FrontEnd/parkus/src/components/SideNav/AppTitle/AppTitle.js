import './AppTitle.css'
import {Typography} from "@mui/material";

function AppTitle() {
    return (
        <Typography fontFamily={"Orelega One"} variant={"h1"} className="appTitle">Parkus</Typography>
    );
}

export default AppTitle;
